{
  // next/babel will block next/core-web-vitals to show
  // error on build, if removing next/babel shows error
  // on every file open, disable your vscode eslint 
  // extension to fix.
  // "extends": ["next/babel", "next/core-web-vitals"]
  "extends": ["next/core-web-vitals"],
  "rules": {
    "@next/next/no-img-element": "off"
  }
}
